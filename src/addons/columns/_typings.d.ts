/**	Options for the columns add-on. */
interface mmOptionsColumns {
	add 		: boolean
	visible		: mmOptionsColumnsVisible
}

/**	"visible" options for the columns add-on. */
interface mmOptionsColumnsVisible {
	min	: number
	max	: number
}
